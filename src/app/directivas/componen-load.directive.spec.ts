import { ComponenLoadDirective } from './componen-load.directive';

describe('ComponenLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponenLoadDirective();
    expect(directive).toBeTruthy();
  });
});
